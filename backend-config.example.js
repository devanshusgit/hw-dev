window.HWDEV_BACKEND = {
  provider: 'local',
  // Change provider to 'supabase' when ready.
  supabaseUrl: '',
  supabaseAnonKey: '',
  // Example admin credentials for local mode only.
  adminUsername: 'admin',
  adminPassword: 'hwdev123'
};
